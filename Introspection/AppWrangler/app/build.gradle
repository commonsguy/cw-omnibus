apply plugin: 'com.android.application'

android {
    compileSdkVersion 'android-O'
    buildToolsVersion "25.0.2"

    defaultConfig {
        minSdkVersion 'O'
        targetSdkVersion 'O'
    }
}
